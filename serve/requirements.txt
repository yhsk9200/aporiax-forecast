# 서빙 의존성 (ADR-0010 D1). scikit-learn·numpy·pandas 버전은 학습과 반드시
# 일치 — baked 모델(pickle)을 그 버전으로 언피클해야 한다. mlflow 없음(런타임에
# MLflow/NAS를 보지 않는다는 원칙 — 모델은 이미지에 구워져 있다).
fastapi==0.115.6
uvicorn==0.34.0
scikit-learn==1.6.1
numpy==2.1.3
pandas==2.2.3
prometheus-api-client==0.5.7
prometheus-client==0.21.1
joblib==1.4.2
